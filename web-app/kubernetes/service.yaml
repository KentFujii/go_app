apiVersion: v1
kind: Service
metadata:
  name: web-app
  labels:
    app: web-app
spec:
  clusterIP: None
  ports:
    - port: 50006
      targetPort: 50006
      protocol: TCP
      name: web-app
  selector:
    app: web-app
