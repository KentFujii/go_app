apiVersion: v1
kind: Service
metadata:
  name: primary
  labels:
    app: primary
spec:
  clusterIP: None
  ports:
    - port: 50001
      targetPort: 50001
      protocol: TCP
      name: primary
    - port: 9001
      targetPort: 9001
      protocol: TCP
      name: primary-sidecar-fluentd
  selector:
    app: primary
